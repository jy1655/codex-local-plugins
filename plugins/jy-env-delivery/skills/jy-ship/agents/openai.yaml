interface:
  display_name: "JY Ship"
  short_description: "Verified push + PR/MR workflow for a ready branch"
  default_prompt: "Use $jy-ship to sync affected docs, review and verify the final candidate, then push and create or update its PR/MR."
