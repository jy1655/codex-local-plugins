interface:
  display_name: "JY Systematic Debugging"
  short_description: "Debug bugs and failing tests through reproduction, hypotheses, and verified root-cause fixes"
  default_prompt: "Use $jy-systematic-debugging to debug this bug or failing test with verified root-cause analysis before patching."
